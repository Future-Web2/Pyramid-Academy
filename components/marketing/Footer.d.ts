import * as React from "react";

/**
 * Footer — site-wide footer on the navy felt backdrop.
 */
export interface FooterProps {
  style?: React.CSSProperties;
}

export function Footer(props: FooterProps): JSX.Element;
