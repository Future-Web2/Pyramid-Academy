# Navbar

**What & when:** The sticky top nav for every marketing page. White-blur on scroll, navy wordmark on the left, links centered, language pill + primary CTA on the right. Always exactly one CTA in the nav.

```jsx
<Navbar currentLang="UZ" onLangChange={(l) => setLang(l)} />
```
